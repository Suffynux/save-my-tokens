import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the on-screen development indicator (the floating Next.js logo that
  // shows the framework version while running the dev server).
  devIndicators: false,
};

export default nextConfig;
